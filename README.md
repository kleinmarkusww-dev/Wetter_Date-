# Wetterblick

Eine kleine Python-Flask-App, die aktuelle Wetterdaten über die Open‑Meteo API anzeigt.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Starten

```bash
python app.py
```

Danach im Browser öffnen: `http://localhost:8000`
